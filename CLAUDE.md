# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository (`moodflow`) is currently empty — no source code, configuration, or tooling has been committed yet. This file was created as an initial scaffold.

Once the project is bootstrapped, update this file with:

- **Build / test commands** — how to install dependencies, run the dev server, execute tests, run a single test file, and lint.
- **Architecture overview** — the high-level structure (e.g. frontend framework, backend language/framework, database, API layer) and how the major pieces connect.
- **Key conventions** — naming patterns, file organisation rules, state management approach, API contract style, or anything non-obvious that a new contributor would need to know.
